[project]
name = "backend"
version = "0.1.0"
description = "Crime Recording Assessment App — backend"
requires-python = ">=3.12"
dependencies = [
    "aiosqlite>=0.22.1",
    "fastapi>=0.138.0",
    "python-multipart>=0.0.32",
    "uvicorn[standard]>=0.49.0",
]
